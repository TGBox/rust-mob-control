[package]
name = "rust-mob-control"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"
rand = "0.8"

# Enable a small amount of optimization in debug mode for smooth ECS crowd simulation
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
